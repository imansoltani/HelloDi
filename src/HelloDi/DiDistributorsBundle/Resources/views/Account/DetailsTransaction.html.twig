
    <div class="span" >
        <div class="widget stacked ">
            <div class="widget-content">
                <a class="btn-large button btn btn-primary " href="#">
                    Recharge
                </a>

                <div class="form_search">
                    <h5 style="cursor:pointer;" class="TitleShow1">
                        <span>
                            <i id="search_logo" class="icon-search"></i>
                            <p class="title_search">Filter List</p>
                        </span>
                    </h5>
                                  </div>

                <div class="tablediv">

                    <div class="contenttitle">
                        <h4>History</h4>
                    </div>
                    <form>
                        <div class="tab-content">

                                    {{ 'id' | trans ({}, "account") }}:{{ tran.id}}</br>
                                    {{ 'Date' | trans ({}, "account") }}:{{ tran.tranDate|date('y-M-d') }}</br>
                                    {{ 'Description' | trans ({}, "account") }}: {{ tran.tranDescription}}</br>
                                    {{ 'Amount' | trans ({}, "account") }}:{{ tran.tranAmount}}</br>
                                    {{ 'Balance' | trans ({}, "account") }}: {{ tran.Account.accBalance }}</br>

                            {{ 'Action' | trans ({}, "account") }}:
                            {% if tran.tranAction=='Sale' %}Sale
                        {% elseif tran.tranAction=='Tran' %}Transfer
                        {% elseif tran.tranAction=='Paym' %}Payment
                        {% elseif tran.tranAction=='Regis' %}Register
                        {% elseif tran.tranAction=='Fund' %}Funding
                            {% elseif tran.tranAction=='CreditNote' %}CreditNote
                            {% endif %}</br>
                             {{ 'User' | trans ({}, "account") }}: {{ tran.User.username }}</br>
                            {{ 'ItemName' | trans ({}, "account") }}: {{ tran.Code.Item.itemName }}</br>
                            {{ 'Accountname' | trans ({}, "account") }} :  {{ tran.Account.accName }}</br>



                        </div>
                    </form>
                </div>

            </div>
        </div>
    </div>
