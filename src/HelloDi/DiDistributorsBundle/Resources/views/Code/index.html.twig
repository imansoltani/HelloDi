{% extends 'HelloDiDiDistributorsBundle:Menu:Master.html.twig' %}

{% block title %}
    Codes
{% endblock title %}

{% set MU = 'code' %}

{% block Site_Map %}
    <a class="link" href="{{ path('loginsuccess') }}">Home</a>
    <span class="link">Code</span>
{% endblock Site_Map %}

{% block PageTitle %}
    <i class="icon-list-alt"></i>
    Code Management
{% endblock %}

{% block Body %}

    <div class="span">
        <div class="widget stacked ">
            <div class="widget-content">

                <div class="form_search">
                    <h5 style="cursor:pointer;" class="TitleShow1">
                        <span>
                            <i id="search_logo" class="icon-search"></i>
                            <p class="title_search"></p>
                        </span>
                    </h5>

                    <form class="form-horizontal SearchForm" action="#" method="post" {{ form_enctype(form) }}>
                        <fieldset>
                            <div class="control-group">
                                {{ form_label(form.provider) }}
                                <div class="controls">
                                    {{ form_widget(form.provider) }}
                                </div>
                            </div>
                            <div class="control-group">
                                {{ form_label(form.item) }}
                                <div class="controls">
                                    {{ form_widget(form.item) }}
                                </div>
                            </div>
                            <div class="control-group">
                                {{ form_label(form.inputFileName) }}
                                <div class="controls">
                                    {{ form_widget(form.inputFileName) }}
                                </div>
                            </div>
                            <div class="control-group">
                                {{ form_label(form.serial) }}
                                {{ form_widget(form.serial) }}
                            </div>
                            <div class="control-group">
                                {{ form_label(form.pin) }}
                                {{ form_widget(form.pin) }}
                            </div>
                            <div class="control-group">
                                {{ form_label(form.status) }}
                                <div class="controls">
                                    {{ form_widget(form.status) }}
                                </div>
                            </div>
                            <div class="control-group datepicker1">
                                {{ form_label(form.insertdate) }}

                                    {{ form_widget(form.insertdate) }}

                            </div>
                            <div class="control-group datepicker2">
                                {{ form_label(form.expiredate) }}

                                    {{ form_widget(form.expiredate) }}

                            </div>
                            {{ form_rest(form) }}
                            <hr>
                            <input type="hidden" name="page" id="search_page" value="1"/>
                            <input class="btn btn-primary" type="submit" value="Search">
                        </fieldset>
                    </form>
                    <script type="text/javascript">
                        $(function () {

                            Form_Search(0,"Search Code");

                        });
                    </script>

                </div>
                <div class="tablediv">

                    <div class="contenttitle">
                        <h4>Codes (Quantity: {{ count }})</h4>
                    </div>
                        <div class="tab-content">
                            <table style="margin-top: 20px"
                                   class="table table-bordered table-striped table-highlight postsend">
                                <thead>
                                <tr>
                                    <th>Item Name</th>
                                    <th>serialNumber</th>
                                    <th>PinCode</th>
                                    <th>ExpiryDate</th>
                                    <th>Status</th>
                                    <th>Provider</th>
                                    <th>Manage</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for code in pagination %}
                                    <tr>
                                        <td>{{ code.Item.itemName }}</td>
                                        <td>{{ code.serialNumber }}</td>
                                        <td>{{ code.pin }}</td>
                                        <td>{{ code.Input.dateExpiry|date('Y/m/d') }}</td>
                                        <td>{% if code.status==1 %}Available{% elseif code.status==0 %}Not Available{% else %}Removed{% endif %}</td>
                                        <td>{{ code.Input.Account.accName }}</td>
                                        <td>
                                            <a href="{{ path('code_history',{'id':code.id}) }}" class="btn_actions">History</a>
                                            <br/>
                                            {% if code.status==0 %}
                                                <a href="javascript:;" onClick="YesNoMessage('Alert', 'Are you sure to Credit Node the code?', '{{ path('code_creditnote',{'id':code.id}) }}')" class="btn_actions">Credit Note</a>
                                            {% elseif code.status==1 %}
                                                <a href="javascript:;" onClick="YesNoMessage('Alert', 'Are you sure to Dead Beat the code?', '{{ path('code_deadbeat',{'id':code.id}) }}')" class="btn_actions">Dead Beat</a>
                                            {% endif %}
                                            <br/>
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>

                        </div>
                </div>
                <div class="navigation" style="text-align: center">
                    {{ knp_pagination_render(pagination) }}
                </div>
                <br/>
            </div>
        </div>
    </div>


{% endblock %}
