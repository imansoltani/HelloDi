        <a  href="{{ path('RetailerUserAdd',{'id':Account.id}) }}">Add New user</a><br>
            <table style="margin-top: 20px" class="table table-striped table-bordered postsend">
                <thead>
                <tr>
                    <th>{{ 'User.Id' | trans ({}, "user") }}</th>
                    <th>{{ 'User.Name' | trans ({}, "user") }}</th>
                    <th>{{ 'User.UserName' | trans ({}, "user") }}</th>
                    <th>{{ 'User.Role' | trans ({}, "user") }}</th>
                    <th>{{ 'User.Modify' | trans ({}, "user") }}</th>
                    <th>{{ 'User.ChangeRole' | trans ({}, "user") }}</th>
                </tr>
                </thead>
                <tbody>

                {% for User in pagination %}
                        <tr>

                            <td >{{ User.id }}</td>
                            <td>{{ User.firstName }}</td>
                            <td>{{ User.username }}</td>
                            <td>
                                {{ User.roles[0] }}
                            </td>

                            <td class="td-actions" >
                                <a class="btn_actions" href="{{ path('RetailerUserEdit',{'id':User.id}) }}">Edit</a>
                            </td>


                            <td class="td-actions" >
                                <a class="btn_actions" href="{{ path('RetailerUserChangeRole',{'id':User.id}) }}">ChangeRole</a>
                            </td>
                        </tr>

                    {% endfor %}

                </tbody>
            </table>
            {#{button pagination}#}
            <div class="navigation">
                {{ knp_pagination_render(pagination) }}
            </div>
